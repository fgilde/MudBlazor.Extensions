@inherits MudExJsRequiredBaseComponent<MudExFileDisplayMarkdown>

@if (!string.IsNullOrEmpty(Value) && _jsReady)
{
    @*TODO: Ask author of MudMarkdown if he can remove the sealed and then inherit from it*@
    <MudMarkdown TableCellMinWidth="@TableCellMinWidth" 
                 Styling="@Styling" 
                 OverrideLinkUrl="@OverrideLinkUrl" 
                 OverrideHeaderTypo="@OverrideHeaderTypo" 
                 LinkCommand="@LinkCommand" 
                 CodeBlockTheme="@CodeBlockTheme" Value="@(Value)"></MudMarkdown>
}
