@inherits MudExJsRequiredBaseComponent<T>
@typeparam T
@typeparam TFile

@if (IsReady && !IsLoading)
{
    <MudButton Variant="@Variant"
               Color="@Color"
               Size="@Size"
               StartIcon="@StartIcon" OnClick="@Pick">
        @ChildContent
    </MudButton>
}
else
{
    <MudProgressCircular Size="@Size" Color="@Color" Indeterminate="true" />
}

