@inherits MudExJsRequiredBaseComponent<MudExOneDriveFilePicker>

@* <style>
    .picker-dialog, .picker-dialog iframe {
        z-index: 9999 !important; /* Use a value that works for your layout */
    }
</style> *@

@if (IsReady && !IsLoading)
{
    <MudButton Variant="@Variant"
               Color="@Color"
               Size="@Size"
               StartIcon="@StartIcon" OnClick="@Pick">
        @ChildContent
    </MudButton>
}
else
{
    <MudProgressCircular Size="@Size" Color="@Color" Indeterminate="true" />
}
