@inherits MudExBaseComponent<MudExPromptDialog>

<MudDialog Style="@Style" Class="@Class">
    <TitleContent>
        <MudText Typo="Typo.h6">
            <MudExIcon Color="IconColor" Icon="@Icon" Class="mr-3 mb-n1"/>
            @MudDialog.Title
        </MudText>
    </TitleContent>
    <DialogContent>
        <MudText>@Message</MudText>
        <MudTextField OnKeyUp="@OnKeyUp" Immediate="@Immediate" @bind-Value="@Value"></MudTextField>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">@TryLocalize(CancelText)</MudButton>
        <MudButton Disabled="@(!IsValid())" Color="Color.Error" Variant="Variant.Filled" OnClick="@(Submit)">@TryLocalize(OkText)</MudButton>
    </DialogActions>
</MudDialog>
