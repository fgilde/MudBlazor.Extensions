@page "/test"
@using FluentValidation
@using BlazorJS
@using MudBlazor.Extensions.Components.ObjectEdit.Options


<DemoComponent PageType="@GetType()" Title="MudExImageViewer">
    <SplitContainer>
        <Left>
            <MudExAudioPlayer @ref="component" Src="/sample-data/electro-psycho-ibiza-no-vocal-sample-edit-169343.mp3" />
        </Left>
        <Right>
            <ComponentPropertyGrid MetaConfiguration="@Configure()" Value="@component"></ComponentPropertyGrid>
        </Right>
    </SplitContainer>
</DemoComponent>

@code {
    
    MudExAudioPlayer component;
    private Action<ObjectEditMeta<MudExAudioPlayer>> Configure()
    {
        return meta =>
        {
            // meta.Property(m => m.ZoomOnHover).Ignore();
        };
    }
}

