@page "/edit-component"
@inherits BasePage
@using MudBlazor.Extensions.Components.ObjectEdit.Options

@attribute [Demo(IsPlaygroundDemo = true, Group = "MudExObjectEdit", Name = "Edit Running Component", Order = 6, Documentation = "Here you can see how you can use the MudExObjectEdit to edit the properties of a running component", Icon = Icons.Material.Outlined.SettingsInputComponent, ForComponentTypes = new[] { typeof(MudExObjectEdit<>), typeof(MudExObjectEditForm<>) })]

<DemoComponent PageType="@GetType()">
    <SplitContainer>
        <Left>
            <MudColorPicker @ref="component" PickerVariant="PickerVariant.Static" />
        </Left>
        <Right>
            <MudExObjectEdit MetaConfiguration="@Configure()" Value="@component"></MudExObjectEdit>
        </Right>
    </SplitContainer>
</DemoComponent>


@code {
    MudColorPicker component;

    private Action<ObjectEditMeta<MudColorPicker>> Configure()
    {
        return meta =>
        {
            meta.IgnoreAllReadOnlyFields();
        };
    }

}