@page "/tree-view-item-tpl"
@inherits BasePage

@using MainSample.WebAssembly.Examples.Tree

@attribute [DemoNew(IsPlaygroundDemo = true, Name = "MudExTreeView with template", Order = 7, Group = "TreeView", Icon = Icons.Material.Outlined.AccountTree, Documentation = "This sample demonstrates the usage of MudExTreeView with a custom Item template", ForComponentTypes = new[] { typeof(MudExTreeView<>) })]

<MudExDemoPage>

    <MudExDemoBox Title="Tree view with custom item template"
                  NavTitle="Custom item template"
                  FeaturesTitle="Configuration"
                  Features="@(new[]
                            {
                                "ItemContentTemplate for custom rendering",
                                "ExpandButtonDirection control",
                                "View mode switching",
                                "Full control over node appearance"
                            })">

        <DemoContent>
            <MudText Typo="Typo.body2" Class="mb-4">
                @L["This example demonstrates how to use a custom ItemContentTemplate to control how tree nodes are rendered. You can style nodes differently, add custom icons, or display additional information for each node."]
            </MudText>
            <TreeViewItemTemplateExample/>
        </DemoContent>
    </MudExDemoBox>

</MudExDemoPage>

<DemoNavigation CurrentPage="@this" />

@code {

}