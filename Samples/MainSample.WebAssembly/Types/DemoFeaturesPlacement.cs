namespace MainSample.WebAssembly.Shared;

/// <summary>
/// Where a <see cref="MudExDemoBox"/> puts its feature list.
/// </summary>
public enum DemoFeaturesPlacement
{
    /// <summary>In a column right of the demo. The default.</summary>
    Beside,

    /// <summary>Under the demo, so the demo itself gets the full width.</summary>
    Below,

    /// <summary>Not rendered at all.</summary>
    None
}
